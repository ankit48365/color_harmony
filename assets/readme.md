## assets

Place sample `.jpg` files or reference screenshots here when you want quick manual checks for the Gradio UI after changes to `src/load_image.py`, `src/build_histogram.py`, or `src/gradio_app.py`.
